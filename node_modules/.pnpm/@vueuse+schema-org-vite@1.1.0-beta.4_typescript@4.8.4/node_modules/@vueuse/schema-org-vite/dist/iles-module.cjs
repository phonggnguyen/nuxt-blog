'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const schemaOrg = require('@vueuse/schema-org');
const SchemaOrg = require('@vueuse/schema-org/vite');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e["default"] : e; }

const SchemaOrg__default = /*#__PURE__*/_interopDefaultLegacy(SchemaOrg);

function schemaOrgIles(pluginOptions) {
  return {
    name: "@vueuse-schema-org/schema-org-iles",
    components: {
      resolvers: [
        schemaOrg.SchemaOrgResolver()
      ]
    },
    config(config) {
      config.vite = config.vite || {};
      config.vite.plugins = config.vite.plugins || [];
      config.vite.plugins.push(SchemaOrg__default({
        full: false,
        dts: true,
        mock: false,
        ...pluginOptions
      }));
    }
  };
}

exports.schemaOrgIles = schemaOrgIles;
