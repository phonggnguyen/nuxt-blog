import { Transformer } from './types.js';
import { ParserOptions } from '@babel/parser';
import { Arrayable } from '@antfu/utils';
import { FilterPattern } from '@rollup/pluginutils';
import '@babel/types';

interface Options {
    include?: FilterPattern;
    exclude?: FilterPattern | undefined;
    enforce?: 'post' | 'pre' | undefined;
    parserOptions?: ParserOptions;
    transformer?: Arrayable<Transformer<any>>;
}
declare type OptionsResolved = Omit<Required<Options>, 'transformer'> & {
    transformer: Transformer<any>[];
};
declare function resolveOption(options: Options): OptionsResolved;

export { Options, OptionsResolved, resolveOption };
