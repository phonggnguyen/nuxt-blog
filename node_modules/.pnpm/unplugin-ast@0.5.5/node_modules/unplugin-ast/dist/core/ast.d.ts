import { Awaitable } from '@antfu/utils';
import { Program, Node } from '@babel/types';
import { ParserOptions } from '@babel/parser';

declare const parseCode: (code: string, id: string, parserOptions: ParserOptions) => Program;
declare const walkAst: (node: Node, callback: (node: Node, parent: Node, index: number) => Awaitable<void>) => Promise<void>;

export { parseCode, walkAst };
